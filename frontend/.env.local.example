# Browser fetches must resolve from the user's machine.
# Local dev:
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
# Inside docker-compose, the browser still hits the host, so keep this as
# the publicly reachable backend URL (NOT http://backend:8000).
